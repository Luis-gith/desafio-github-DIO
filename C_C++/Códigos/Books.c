#include<stdio.h>
#include<stdlib.h>
#include<math.h>
int main(void){
//Declaring Variables
int p;
int pages[p];
int j;
int y;
int i;
int years[y];
int b;
int books[b];
int sum; sum+=pages[p];
{
printf("How many years do you want to compare?\n");
scanf("%d", &y);
for(int i=0; i<y; i++)
{
printf("How many books have read on year %d ?\n",i+1);
scanf("%d",&b);
{
for(int j=0; j<b; j++)
{
printf("And how many pages, on average, book %d has?\n",j+1);
scanf("%d",&p);
}
}
}
printf("You have read an average of %d pages per year.\n ",p/y, );

}
}
